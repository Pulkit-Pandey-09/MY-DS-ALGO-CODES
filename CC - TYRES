#include<bits/stdc++.h>
using namespace std;

typedef long long LL;             // to rename a datatype
#define pi 3.14                  // to replace all pi with 3.14
#define MAX 100005;

int main()
{
    #ifndef ONLINE_JUDGE
    // for getting input from input.txt
    freopen("input.txt", "r", stdin);
    // for writing output to output.txt
    freopen("output.txt", "w", stdout);
   #endif
    int t;
    cin >> t;
    while(t--)
    {
        int n;
        cin >> n;
        int rem = n % 4;
        if(rem >= 2) cout << "YES" << endl;
        else cout << "NO" << endl;
    }

}
