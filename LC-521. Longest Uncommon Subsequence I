class Solution {
public:
 
    int findLUSlength(string a, string b) 
    {
        int len1 = a.length();
        int len2 = b.length();
 
        if(a.compare(b) == 0) return -1;
        return max(len1,len2);
    }
};
